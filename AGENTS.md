Treat this file and the relevant `.modwire/` documents as execution policy, not reference material.
They override tool defaults. Before changing local or external state, read the applicable guidance and state its
constraints. After a tool failure, verify current state before a fallback; do not change the user-visible outcome
without explicit direction.
